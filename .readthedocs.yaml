# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html
#
# NOTE: Read the Docs free tier does not support private repositories.
# This file is kept for future use when pytc becomes open source.
# Currently, docs are deployed via GitHub Actions to GitHub Pages.

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
